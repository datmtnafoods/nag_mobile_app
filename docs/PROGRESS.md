# nag_mobile_app — Nhật ký & trạng thái chức năng

> Tài liệu truy vết chính thức của app KTV (Expo / React Native).
> **git log** là chi tiết kỹ thuật; **PROGRESS.md** là nhật ký người đọc — mỗi khi thêm/đổi chức năng có ý nghĩa, cập nhật bảng dưới **cùng commit** đó.

Cập nhật: 2026-08-22 · Nhánh: `feat/phase3b-vat-tu-auth-location`

## Trạng thái tổng quan

| Mảng | Trạng thái | Ghi chú |
|---|---|---|
| Auth (login, mock mode) | ✅ Done | phase1 — nền tảng auth + tab layout |
| Kích hoạt tem QR | ✅ Done | phase1 — quét QR + luồng activation |
| Đơn hàng giống (orders) | ✅ Done | phase2 — list + detail + wizard tạo + FSM trạng thái |
| Vật tư (nhập/xuất) | ✅ Done | phase3/3b — parity với ERP, catalog, wizard, scan |
| **Bán lẻ vật tư** | ✅ Done (mock) | Scan liên tục, suggestion tồn kho, billing, thanh toán, đổi trả, khách lẻ/HTX — **lớp TIỀN + trả hàng là mock**, BE chưa có |
| **Inbox tin nhắn** | ✅ Done (mock) | Chat 2 chiều quầy ↔ nông hộ/HTX; auto-reply giả; **BE + realtime WS là Phase 3, chưa có** |
| Đến thửa (GPS → tạo thửa → nhật ký) | ⏳ In Progress | Toàn bộ backend đang **mock**; đang gọt UX theo phản hồi thực địa |
| Lịch canh tác theo thửa | ✅ Done (BE thật) | Nối `crop-catalog`/`plot-milestone` + cột `planted_at`; badge giai đoạn + tạo nhanh chọn mẫu. Cây khác vẫn chờ nghiệp vụ cấp mốc |
| **Điều hướng / IA (tab bar)** | ✅ Done | 5 tab (icon filled khi active, nền trắng + hairline): Trang chủ · Tin nhắn (badge) · Bán hàng · Vùng trồng · Cá nhân. **UI de-box** (list-row, hết lưới ô vuông màu): hub = `SectionLabel` + `RowGroup` + `ListRow` (grouped, accent theo nhóm); Trang chủ dọc + `QuickAction` lối tắt; màu chỉ trong icon chip; `BONG` shadow + `theme/tokens.ts` |

## Nhật ký commit (mới → cũ)

| Commit | Loại | Nội dung |
|---|---|---|
| _(mới)_ | feat | `den-thua`: **Lịch canh tác chạy backend THẬT + badge giai đoạn + tạo nhanh chọn mẫu** — trước chạy 100% mock (nhánh real `throw`); nay nối endpoint thật đã dựng ở `nag_erp` (module `crop-catalog` + `plot-milestone` + migration `2026-08-22_lich_canh_tac.sql` thêm cột `growing_plot.planted_at`; verify end-to-end qua HTTP thật + Postgres). (1) **Nối real API**: `lich-cay.ts` (GET/PUT/DELETE `/crop-catalog[/:id]`, 404→null khi cây chưa có lịch) + `canh-tac.ts` (GET/PUT/DELETE `/growing-areas/plots/:plotId/milestones[/:mocId]`) bỏ nhánh throw, GIỮ nhánh `MOCK_API`; `growing-areas.ts` thôi strip `ngayGoc` — gửi cả khi tạo + patch (map cột `planted_at`). (2) **Badge giai đoạn** (module mới `giai-doan.ts`: `giaiDoanHienTai(mocs, idx)` + `ACCENT_LOAI` gom về 1 nguồn): chip màu "Đang: <mốc>" / "Sắp tới: <mốc>" trên card Lịch canh tác ở `thua/[id].tsx` — KTV liếc biết cây đang giai đoạn nào, khỏi rà timeline. (3) **Tạo nhanh** (component chung mới `MauLichSheet.tsx`, 2 lối vào): trên `thua/[id].tsx` nút "Tạo lịch cây" mở bottom-sheet chọn mẫu (clone cấu trúc mốc từ lịch cây đã có / "bắt đầu tối thiểu" 1 mốc — KHÔNG bịa mốc) → lưu liền không rời màn; trong editor `lich-cay/[cayId].tsx` thêm nút "Chọn mẫu" prefill danh sách mốc. `useMocThua` trả thêm `dsLich` để sheet khỏi query lại. (4) **Gọn text/khoảng trống** nhiều màn (empty-state lịch canh tác, ban-hang/kiem-kho/pair-code/tao-thua/lich-cay helper, whitespace profile) — bỏ câu thừa lặp nhãn/nút kề bên. **RBAC** (ở backend DB, không phải code mobile): cấp `crop-catalog:edit` + `plot-milestone:confirm` cho `field_staff` qua `PUT /rbac/role-permissions` (hydrate DB là nguồn sự thật — sửa DEFAULT trong code vô tác dụng trên DB đã seed). `tsc` sạch. |
| _(mới)_ | feat | `vat-tu`: **Tạo kho tạm (xe) nhanh trên app — offline-first, đồng bộ BE sau** — KTV ngoài vườn trước đây bí ở màn Chuyển kho (đích) + Bán hàng khi chưa có xe nào: chỉ hiện dead-end "Yêu cầu admin gắn custodian". Nay tự tạo kho tạm ngay tại chỗ. Kho tạm **= kho xe thật** (`loai:'xe'`, custodian = chính KTV — DB CHECK ép non-null), KHÔNG phải khái niệm client riêng; app tạo rồi đồng bộ lên backend sau để 3 client không lệch. **Nhân bản mẫu offline nông hộ** (`party-queue`+`party-sync`+`giaiQuyetHo`), không tự chế. (1) **API `taoKhoTam`** trong `warehouse.ts` (dual-branch Khuôn 4): mock push `MOCK_KHO` (id `kho_xe_tam_*` qua `nextKhoTamId`) trả dùng được ngay; real `POST /kho` body `{ten,loai:'xe',custodianUserId}` trả Kho trực tiếp — **BE CHƯA có route** nên còn 404, doc-comment ghi nợ + gate quyền BE (không map role client). Type `TaoKhoTamBody` + cờ `Kho.dongBoTam` (display-only) ở `types.ts`. (2) **Queue offline `stores/kho-tam-queue.ts`** (bản sao `party-queue`, persist `nag.kho-tam-queue`, tempId `LOCAL-KHO-<seq>`; không PII nên persist đủ) + `reconcileKhoTamQueueForUser`; đăng ký reset/removeItem ở `wire.ts`, reconcile+`flushKhoQueue` ở `login.tsx`/`register.tsx`. (3) **Drain `api/erp/kho-sync.ts`** (`flushKhoQueue`+`wireKhoSync` gọi ở `_layout.tsx` cạnh `wireOfflineSync`): **GIỮ lại khi 404/50x/không status** (BE chưa ship → mọi lần thử 404, KHÁC party drop lỗi nghiệp vụ — drop sẽ mất kho KTV đã tạo); chỉ drop 400/409. `idThatCuaKhoTam` map tempId→real cho remap phiếu (follow-up). (4) **Hook `useKhoList()`** gộp `listKho` + kho tạm pending của user (badge "chưa đồng bộ"); thay query kho inline ở `chuyen-kho/new.tsx` + `ban-hang/new.tsx`. (5) **Modal `KhoTamQuickCreateModal`** (mirror `NccQuickCreateModal`, chỉ nhập Tên) + mutation "thử-rồi-lùi" (online POST id thật, mất mạng enqueue tempId), tạo xong auto-chọn (`setKhoDich`/`setKho`). (6) **Guardrail**: kho đang chọn là `dongBoTam` → chặn lập lệnh/bán + hint "cần mạng để chốt" (mirror lý do party không dùng tempId ở luồng gán). (7) **Loại xe** (`LoaiXe='xe_may'|'xe_tai'`, `Kho.loaiXe?`): kho tạm = xe của shipper (hàng in-transit, chưa giao tới kho khác) — modal thêm 2 chip Xe máy/Xe tải, chip kho hiển thị icon theo loại (`KhoIcon` dùng `MaterialCommunityIcons` motorbike/truck, fallback `car-outline` cho tổng/trạm dùng Ionicons). (8) **Nới phạm vi chuyển kho (trung chuyển)**: `chuyen-kho/new.tsx` NGUỒN + ĐÍCH đều mở cho MỌI kho (tổng/trạm/xe) — trước nguồn chỉ tổng/trạm, đích chỉ "xe của tôi"; cho **xe làm nguồn** chuyển tiếp sang kho khác, đích bỏ bó custodian (marker `*` = xe người khác); guardrail mở rộng chặn nếu **nguồn HOẶC đích** là `dongBoTam`. Bán hàng giữ nguyên (trạm + xe của tôi). Mock-online dùng full ngay; seam sync sống khi BE có `POST /kho`. `tsc` sạch. |
| _(mới)_ | ux | `den-thua`: **Gọn màn "Lịch cây trồng" — sheet Modal + list nén + Nâng cao ẩn** — sau khi đợt trước chạy thực tế, KTV phản hồi màn quá nhiều trường mỗi mốc. Gọt UX (không đổi data/API): (1) Danh sách mốc = **1 hàng nén / mốc** — dot màu theo loại (`ACCENT` map: kích hoạt-đỏ / kiến thiết-xanh lá / làm bông-tím / đi cành-xanh dương / thu hoạch-hổ phách) + nhãn + "T+N tháng · Lứa X", tap để sửa; nút xoá có Alert confirm. (2) **Sheet Modal thêm/sửa mốc** — 5 chip preset auto điền nhãn/loại/lứa (Thu hoạch tự sinh "Thu hoạch lứa {luaMax+1}"), chỉ 2 field chính (Nhãn + Tháng); "Lứa số" chỉ hiện khi loai=thu_hoach. Chip preset chọn xong đè nhãn nếu ô đang trùng preset trước đó — không đè nhãn user gõ tay. (3) **"Cài đặt nâng cao" collapse** (mặc định thu) gộp Từ khoá nhận diện + Chu kỳ lặp lứa — đa số KTV không đụng. (4) Section "Tên hiển thị" thu về 1 Input đơn (bỏ ô từ khoá khỏi cấp trên). Preview timeline giữ nguyên. Không đổi shape `LichCayTrong`, API `lich-cay.ts`, mock — lịch cũ mở ra & lưu lại đúng payload. `tsc` sạch. |
| _(mới)_ | feat | `den-thua`: **Vụ / lứa thu hoạch gắn timeline** — thêm khối theo dõi lứa ở màn chi tiết thửa + gắn nhật ký `thu_hoach` vào lứa cụ thể. Vụ/lứa **KHÔNG là entity riêng** — là view dẫn xuất từ 3 nguồn: mốc `loai='thu_hoach'` trong mocs (đã merge xác nhận thực tế), nhật ký thu_hoach gắn mocId; một nguồn sự thật, BE sau này có `harvest_cycle` chỉ đổi tầng derive (đánh dấu TẠM theo nguyên tắc 1). (1) `ChiTietThuHoach.mocId?` mới — bền hơn `lua: number` khi lịch cây bị sửa; nhật ký cũ không mocId rơi vào nhóm "Chưa gắn lứa" (không mất). (2) **Helper thuần `vu-lua.ts`**: `tinhLuaThua(mocs, nhatKy)` → `{lua: LuaThua[], chuaGan}`. `LuaThua { mocId, lua, nhan, ngayDuKien, ngayThucTe, sanLuongCongDon, soNhatKy, trangThai }`. Trạng thái: `sap_toi` (chưa tới), `cho_xac_nhan` (tới hạn chưa xác nhận), `da_thu` (có ngayThucTe). (3) **Khối "Vụ / lứa thu hoạch"** trong `thua/[id].tsx` ngay dưới Lịch canh tác: mỗi lứa 1 row với badge trạng thái ACCENT xám/hổ-phách/xanh-lá, "Dự kiến {ngày} → thực tế {ngày}", sản lượng cộng dồn đậm + N bản ghi; tap row → mở sheet xác nhận mốc tương ứng (reuse `moSheet`). Mặc định thu gọn quanh lứa hiện tại ±1 + nút "Xem tất cả N lứa" (chanh leo 8 lứa). Dòng "Chưa gắn lứa: N bản ghi · X kg" khi có. (4) **`ThuHoachForm` nâng cấp**: nhận props `mocThuHoach?: MocCanhTac[]` + `ngay?` — hàng chip cuộn ngang chọn lứa ("Lứa 1"…, "Không gắn lứa"); **auto-chọn mốc có `|ngày nhật ký − ngayDuKien|` nhỏ nhất**, chỉ khi user chưa đụng chip lần nào (cờ `userDaChon` ref). `nhat-ky.tsx` truyền `mocs.filter(loai='thu_hoach')` từ `useMocThua`. (5) Seed mock: 1 nhật ký thu_hoach `GP-260810-01` `mocId=thu_hoach_1` (240 kg, ngày 01/11/2025) — trùng xác nhận mốc muộn 12 ngày sẵn có → khối Vụ/lứa demo trạng thái "Đã thu" + sản lượng cộng dồn khớp. `tsc` sạch. |
| _(mới)_ | feat | `den-thua`: **Lịch canh tác chỉnh được theo LOẠI CÂY** — chuyển từ hằng hardcode `LICH_CANH_TAC` (chỉ chanh leo, sửa buộc đổi code) sang **dữ liệu qua tầng API** — KTV tự khai lịch cho từng loại cây, áp cho MỌI thửa trồng cây đó (giải luôn bài toán cà phê/bơ/ổi chưa có lịch, không bịa data). Không đụng tới per-thửa: xác nhận ngày thực tế mốc vẫn qua sheet cũ ở `thua/[id].tsx`. (1) **API mới `lich-cay.ts`** (khuôn `canh-tac.ts`, real throw `'Backend chưa có API lịch cây trồng.'`, doc-comment nợ BE `crop_catalog`/`crop_stage` §9.6 nag_erp): `listLichCay`/`getLichCay`/`luuLichCay` (upsert)/`xoaLichCay`. Mock `MOCK_LICH_CAY` trong `den-thua.mock.ts`, seed = lịch chanh leo cũ (mocDau 4 + chuKy 2-3 tháng + 8 lứa + goiYTheoGiaiDoan). (2) **Refactor `lich-canh-tac.ts`**: bỏ hằng `LICH_CANH_TAC`; `nhanDangCayTrong(cropName, dsLich)` nhận danh sách làm tham số; `tinhMocCanhTac` thêm nhánh `chuKy` optional (cây 1 lứa/vụ như bơ chỉ trả `mocDau`); thêm helper `slugCay` (id lịch cho cây mới). `LichCayTrong.chuKy?`/`soLuaToiDa?` giờ optional. (3) **Hook mới `useMocThua(thua)`** trả `{lich, mocs, idxHienTai, dangTai}` từ query `['lich-cay']` + `['moc-canh-tac', plotId]` — dùng chung cho `thua/[id].tsx` + `thua/nhat-ky.tsx` (form nhật ký từ nay gợi ý giai đoạn ĂN THEO override lịch, không lệch với timeline chi tiết). `tao-thua.tsx` (preview lịch) cũng đọc `dsLich` từ query. Xác nhận mốc mồ côi (`MOCK_MOC_XAC_NHAN` không khớp mocId sau khi lịch đổi) bị bỏ qua im lặng — không crash, không cần dọn mock. (4) **Màn mới `app/thua/lich-cay/[cayId].tsx`**: header cảnh báo "áp cho MỌI thửa"; section 1 tên + từ khoá nhận diện (comma-separated); section 2 danh sách mốc — mỗi hàng nhãn + chip 5 loại (`nhanLoaiMoc`) + tháng T+n (`useNumericInput` int ≥0) + lứa (nếu `thu_hoach`) + nút xoá ≥44px; section 3 chu kỳ lặp lứa (Switch bật/tắt, thangDiCanh/thangThuHoach/soLuaToiDa ≤20); section 4 **preview `TimelineCanhTac` read-only** tính từ hôm nay, cập nhật sống; nút Xoá lịch (variant=danger) khi đã có; nút Lưu ở footer cố định. Sort mốc theo `thang` khi lưu. Validate: ≥1 mốc, ≥1 từ khoá, nhãn mốc không rỗng, chu kỳ phải neo vào mốc `thu_hoach`. (5) **Lối vào từ `thua/[id].tsx`**: nút pill "Sửa lịch {cây}" (có lịch) hoặc "Tạo lịch cây" (chưa) cạnh tiêu đề khối Lịch canh tác. Đăng ký route ở `app/thua/_layout.tsx`. `tsc` sạch. |
| _(mới)_ | feat | `den-thua`: **Nhật ký gắn SKU vật tư (bón phân / phun thuốc)** — kích hoạt field `dongVatTu` đã khai sẵn ở `NhatKyCanhTac`/`CreateNhatKyBody` nhưng chưa UI nào dùng. (1) **Sheet mới `ChonVatTuSheet.tsx`** (Modal inline, KHÔNG tái dùng `sku-picker` bán hàng vì nó qua draft-store — trade-off ghi trong doc): tìm kiếm debounce, filter mặc định `loai_phan_bon`/`loai_thuoc_bvtv` theo loại nhật ký + FilterChip "Tất cả", **section "Đã mua"** ghim SKU hộ đã mua gần đây (query `listReceipts partyId+ban+ghi`, guard `p.dongHang` — real rows có thể thiếu, KHÔNG N+1 `getReceipt`), badge "Đã mua" xanh; SKU đã thêm vào form disable + chip "Đã thêm". (2) **Block "Vật tư sử dụng"** trong sub-form Bón phân + Phun thuốc: dòng đã chọn (tên + số lượng `useNumericInput` + đơn vị readonly + nút xoá ≥44px), nút "＋ Chọn từ danh mục"; caption ghi rõ "KHÔNG trừ tồn kho". Chặn trùng vatTuId. **Đổi loại nhật ký → reset `dongVatTu`** (tránh gửi phân bón kèm nhật ký phun). (3) **Auto-fill** `tenPhan`/`tenThuoc` khi ô đang trống — không đè lên chữ user đã gõ. (4) Lịch sử (2 màn: nhat-ky + thua/[id]) hiện badge cube + N + dòng "tenSku × SL donVi" tóm tắt. (5) Doc-comment `nhat-ky.ts` ghi BE pass-through nhưng chưa đọc `dongVatTu`; seed 1 nhật ký bón phân có `dongVatTu` (NPK 20-20-15 × 25 kg) để demo section "Đã mua". `tsc` sạch. |
| _(mới)_ | feat/fix | `den-thua`: **Nhật ký sâu bệnh có cấu trúc + ảnh hiện trường + bugfix mốc hiện tại** — (1) Loại `tinh_trang_cay` (trước là "loại mồ côi" — chỉ có ô moTa tự do) nay có `ChiTietTinhTrangCay { doiTuong*, mucDo (Nhẹ/Vừa/Nặng, ACCENT xanh/vàng/đỏ), boPhan[] (Lá/Thân/Cành/Hoa/Quả/Rễ/Gốc, multi-chip), tiLeUocLuong% }` + `TinhTrangCayForm` (khuôn `BonPhanForm`). Thêm vào union `ChiTietNhatKy`; seed record mock có chiTiet mẫu. (2) **Ảnh cho MỌI loại nhật ký** — `nhat-ky.tsx` dùng `ImagePickerRow` (`MAX_ANH_NHAT_KY=4`), thay `anh: []` cứng. Real mode vẫn strip `anh` (BE vứt, có chủ đích); mock lưu đủ. Badge camera + số ảnh ở 2 list lịch sử (`nhat-ky.tsx`+`thua/[id].tsx`). (3) `goiYTheoGiaiDoan` chanh leo thêm `tinh_trang_cay` (kien_thiet/lam_bong/di_canh/thu_hoach) — hết cảnh loại mồ côi luôn rơi "Loại khác". (4) **Bugfix `chiSoMocHienTai`**: khởi tạo `idx = -1` (trước 0 → thửa mới trồng `ngayGoc` tương lai vẫn highlight mốc đầu như "đã qua", form gợi ý sai giai đoạn). `TimelineCanhTac` guard `hienTai >= 0` trước khi so index. `tsc` sạch. WORKFLOW.md W5 cập nhật: app-gap ẢNH đã đóng, còn ghi âm chờ BE whitelist `audio/*`. |
| _(mới)_ | feat | `ui`: **Redesign giao diện — de-box sang list-row hiện đại** — user chê "ô vuông khối gây khó chịu" (lưới card 2 cột pastel, mỗi ô một màu, viền + chữ nhuộm màu). Bỏ hẳn khuôn đó. (1) **Xoá `HubCard`/`RailCard`**; thêm **`RowGroup`** (grouped-card kiểu iOS Settings: 1 viền + divider giữa các row, có bóng `BONG`) + **`QuickAction`** (lối tắt 4 cột icon-chip). (2) **`ListRow` nâng cấp** làm khuôn hub chính: props `grouped`/`size='lon'`/`enabled`/`permLabel` — bề mặt trắng, màu CHỈ trong icon chip, chữ đen/xám. (3) **Chuẩn hoá tokens**: bảng `ACCENT` đồng đều MỘT nấc (bg-100/border-200/text-700 — trước xanh-la/ho-phach đậm lệch); thêm `BONG.card` (shadow mềm đa nền tảng, spread vào `style`), `rounded-card-lg` 16, `primary.200`; `SectionLabel` → 12px semibold + tracking; `Button` thêm variant `danger`, bỏ hardcode. (4) **Trang chủ** bố cục dọc lấp màn: greeting + ngày + avatar, "Cần xử lý" list dọc (rỗng → card "Không có việc cần xử lý"), "Đang làm dở", **Lối tắt QuickAction 4 cột**, "Phiếu gần đây" (tái dùng cache listReceipts). (5) **vung-trong/kho** chuyển lưới → 2–3 nhóm `RowGroup`, accent theo nhóm (hết cầu vồng). (6) **Tab bar**: nền trắng + hairline, **icon filled khi active**, label 11px. (7) **Profile**: avatar tròn (bỏ squircle đỏ đặc), card info có divider, khối Backend chỉ hiện `__DEV__`, nút Đăng xuất `variant=danger`. Cập nhật `SKILL.md` theo design mới. `tsc` sạch. |
| _(mới)_ | feat | `inbox`: **Nâng cấp toàn diện Tin nhắn** — (1) **Chắc luồng gửi**: optimistic bubble "Đang gửi" bằng overlay state cục bộ (KHÔNG dùng query cache vì poll 4s xoá mất), `onSuccess` chèn tin thật vào cache + gỡ optimistic (không nháy/trùng), `onError` → bubble "Lỗi — Gửi lại" (giữ nội dung, tap gửi lại) — hết cảnh xoá input trước mutate rồi mất tin. (2) **Đủ 3 trạng thái màn chat** (thêm error `ErrorState` + empty), composer luôn ở đáy. (3) **Layout list**: bỏ trùng tiêu đề "Tin nhắn" (giữ header Stack, bỏ h1 in-body) + `edges` bottom (hết double top-pad); thêm **tìm kiếm** (debounce) + **pull-to-refresh** + EmptyState có CTA. (4) **Thời gian tương đối** (`thoiGian.ts`: "8:15"/"Hôm qua"/"T2") + **separator ngày** trong chat. (5) **Card đính kèm giàu thông tin**: type `PhieuSnapshot` (số tiền/còn nợ/ngày/số mặt hàng) — 2 site gửi (nhắc nợ, hoá đơn) build từ `PhieuBan`+`payment.ts`; card render có cấu trúc, fallback `noiDung` cho tin cũ. (6) **Typing indicator** + **"Đã xem"** (mock heuristic, doc-comment ghi rõ giả lập; Phase 3 WebSocket mới thật). (7) **Nháp khi gõ** RAM-only (`inbox-draft.ts`, Nhóm A — text chat là PII nên KHÔNG persist; đăng ký `reset` ở `wire.ts`+`profile.tsx`). (8) **Tách 7 component** `features/inbox/components/` (HoiThoaiRow/MessageBubble/PhieuCardChat/DaySeparator/TypingBubble/MessageStatus/ChatComposer) + dọn hardcode hex sang `MAU`/`ACCENT`. Backend vẫn Phase 3 chưa có — toàn bộ client+mock. `tsc` sạch. |
| _(mới)_ | feat | `den-thua`: **Bản đồ thửa (kiểu Booking) trong màn Thửa** — tab `den-thua` thêm toggle **Danh sách ↔ Bản đồ** ([FilterChip](src/components/FilterChip.tsx)); chế độ bản đồ vẽ **tất cả thửa của nông hộ** dưới dạng polygon trên ảnh vệ tinh, **nhãn = tên hộ** (fallback mã thửa), màu theo trạng thái duyệt (xanh/hổ-phách/xám từ `ACCENT`). Tap thửa → **card nổi dưới** ([ThuaDatCard](src/features/den-thua/components/ThuaDatCard.tsx)) + highlight + `easeTo`; tap chỗ trống → đóng card; tap card → chi tiết `/thua/[id]`. Tìm kiếm (debounce 250ms) lọc polygon + **tự căn khung** về nhóm khớp. **Tái dùng đúng map hệ thống** (MapLibre-in-WebView) — mở rộng [banDoHtml.ts](src/features/den-thua/components/banDoHtml.ts)+[BanDoRanh.tsx](src/features/den-thua/components/BanDoRanh.tsx) cộng dồn (bản tin mới `setPlots`/`focusPlot`/`plotTap`/`mapTap`, layer `plots-*`+`plot-labels` đọc glyph runtime để có dấu tiếng Việt, guard GPS-jump khi có plots), **không thêm thư viện map**; 3 call-site vẽ/xem cũ không đổi. Predicate lọc + màu status tách [timKiem.ts](src/features/den-thua/timKiem.ts) dùng chung 2 chế độ. Bản đồ hỏng (mất mạng/CDN) → rơi về danh sách thuần + nút Thử lại (remount). `tsc` sạch. **Lưu ý real mode**: BE lọc thửa theo người tạo nếu thiếu `growing-area:approve` → bản đồ có thể thiếu thửa người khác (xem Follow-ups). |
| _(mới)_ | feat | **Thiết kế lại điều hướng: 5 tab + Trang chủ + chuẩn UI dùng chung** — gom IA về **Trang chủ · Tin nhắn · Bán hàng · Vùng trồng · Cá nhân**. (1) **Trang chủ mới** [(tabs)/index.tsx](app/(tabs)/index.tsx) (trước chỉ redirect) — gọn, **3 rail cuộn ngang** (bố cục ngang chỉ áp dụng ở màn này): _Cần xử lý_ (tin chưa đọc, phiếu nhập `ke_hoach`, lệnh chuyển chờ nhận/duyệt lệch — gate theo quyền), _Đang làm dở_ (đọc 4 draft-store, chạm để tiếp tục — **không** `startDraft` để khỏi wipe), _Lối tắt_. Thẻ rail dùng component mới [RailCard.tsx](src/components/RailCard.tsx). _(Bỏ khối "Gần đây" + "Phiếu mới nhất" cho gọn; store `hoat-dong-gan-day` từng thêm cho "Gần đây" đã revert — xem Follow-ups.)_ (2) **Tab Bán hàng** = hub `kho` retitle, chia 3 nhóm, **thêm card Chuyển kho** (W7 trước không có lối vào) + Đơn hàng giống + Quét tem (gỡ mồ côi). (3) **Tab Vùng trồng** = hub mới gộp Thửa + Nông hộ; `den-thua`/`nong-ho` thành route ẩn (nút quay lại về hub). (4) **Badge chưa đọc** trên tab Tin nhắn (dùng chung queryKey với màn inbox). (5) **Chuẩn UI xuyên suốt**: [theme/tokens.ts](src/theme/tokens.ts) (`MAU`/`ICON`/`ACCENT`) + component dùng chung `HubCard`/`ListRow`/`RailCard`/`SectionLabel` — hết hardcode hex per-card. Route giữ tên cũ (`kho`/`den-thua`/`nong-ho`) nên fallback/deeplink không vỡ. `tsc` sạch. |
| _(mới)_ | feat | `vat-tu`+`inbox`: **Bán lẻ vật tư đầy đủ + inbox khách hàng (mock-first)** — nâng luồng bán từ "ghi phiếu xuất" lên bán lẻ tại quầy. (1) **Scan liên tục** (`scan-code.tsx` param `mode=lien_tuc`): quét nhiều món không đóng camera, rung + toast tên/giá + đếm "Xong (N món)", mã lạ → banner "Gắn mã" (không Alert chặn); port `autofocus`/zoom/`onDismiss` từ khuôn `quet-cccd`. (2) **Suggestion tồn kho** (`sku-picker`): nhận `khoId` → `tonKho()` 1 request, `StockBadge` mỗi SKU, SKU còn tồn nổi lên trước, search debounce (`useDebouncedValue` mới, retrofit `customer-picker`). (3) **Billing**: sửa đơn giá dòng (`LinePriceSheet` + prop `onEditGia`) + trong picker, giảm giá toàn phiếu (mock `createPhieuBan` nay trừ `giamGia`), tiền khách đưa/thối (không lưu), **màn hoá đơn** `ban-hang/hoa-don/[id]` chụp ảnh chia sẻ (`react-native-view-shot`+`expo-sharing`). (4) **Thanh toán** (`payment.ts`+`thanh-toan.ts`, mock): trạng thái derive đã-tt/một-phần/ghi-nợ, phương thức TM/CK, `LanThu`, nút **Thu thêm** (`ThuTienSheet`), badge ở `PhieuCard`/detail. (5) **Khách lẻ** vãng lai (không partyId, PII không persist) + **bán cho HTX** (partyKind `cooperative`, picker list riêng). (6) **Đổi trả một phần** (`phieu-tra.ts`, mock): từ phiếu gốc chọn dòng+SL+lý do → phiếu `khach_tra` nhập lại kho, trừ nợ trước hoàn sau, chặn `tra_vuot_so_luong`. (7) **Inbox** (`features/inbox/*`, `api/erp/inbox.ts`, tab `(tabs)/inbox`): chat 2 chiều, card hoá đơn/nhắc nợ mở phiếu, nút Nhắn tin/Nhắc nợ/Gửi hoá đơn từ luồng bán, role `htx` xem đảo phía. Vá kèm: bug tiền `LineEditor` (suy đơn vị từ `caption`), drift envelope 10 call site `catalog-supplies.ts`. `receipt-draft` bump v2→v3. `tsc` sạch. |
| _(mới)_ | fix | `vat-tu`: **Bỏ role ma, map quyền kho theo `permissions` của backend** — `features/vat-tu/perms.ts` tự map role→quyền bằng bộ role **KHÔNG TỒN TẠI ở backend** (`kho_manager`, `kho_staff`, `tram_staff`, `staff`) và còn cấp `kho:view` cho `npp` — thứ `core/rbac.js` không cho ⇒ UI mở màn rồi API trả 403. Nay đọc thẳng `permissions` backend trả lúc đăng nhập (giống `den-thua/perms.ts` vốn đã làm đúng và đã cảnh báo về file này trong doc-comment); thêm hook `usePermissions()`; đổi 12 call site từ `permsForVatTu(user?.roles)` sang `permsForVatTu(permissions)`. Đồng bộ mock `auth/roles.ts` với `core/rbac.js` (`field_staff`/`sales_staff` thiếu `party:*`+`task:*`, `npp` thiếu `party:view`). Dọn comment stale ở `den-thua/perms.ts` (`field_staff` NAY CÓ `party:create`). **Phát hiện kèm: `kho:ban` không thuộc vai nào ngoài admin** — xem Follow-ups. `tsc` sạch. |
| _(mới)_ | fix | `vat-tu`: **Vá lệch envelope response — mở khoá TOÀN BỘ module kho ở real mode** — mobile đọc `data.data` ở **17/17** call site trong `warehouse.ts`, nhưng backend **không bọc `{data}`** ở bất kỳ route nào: list trả `{rows,total}` (`core/list.js`), create/xác-nhận/huỷ trả `{phieu,moves}`, patch kế hoạch trả `{phieu}` ⇒ mọi hàm real mode trả **`undefined`** (nhập, bán, tồn kho, kiểm kho, moves, list phiếu — tất cả). Kiểm chứng bằng gọi thật qua tunnel: `GET /kho` → `{"rows":[...],"total":N}`. Sửa: thêm type `BeList<T>` đọc `rows`; tách helper `toPhieuFull()` (enrich snapshot dòng hàng, dùng chung mock+real) + `boc()` dựng `PhieuFull` từ `{phieu}` — lưu ý `GET /phieu/:id` của BE trả `dongHang` là **dòng sổ (moves)**, không phải dòng nhập liệu UI cần, nên luôn dựng lại từ `phieu.dongHang` snapshot. `tsc` sạch. |
| _(mới)_ | feat/fix | `vat-tu`: **Bán vật tư gắn hồ sơ nông hộ + khớp hợp đồng backend** — (1) **Sửa lệch hợp đồng**: mobile gửi `nongHoId`/`nongHoTen` trong khi BE `kho/service.js` yêu cầu **`partyId`** (bắt buộc, thiếu → 400 `thieu_khach_hang`) + `partyName`/`partyKind`/`giamGia` ⇒ real mode POST phiếu bán **đang gãy**; đã đổi tên field ở `types.ts` (`PhieuBan`, `CreateReceiptBody`, `ListReceiptsQuery`+`partyId`), `receipt-draft.ts`, `warehouse.ts` (mock + real). (2) **Bỏ "Khách lẻ"** theo BE (đổi 2026-08-19) — mọi phiếu bán phải gắn hồ sơ; mock thêm guard `thieu_khach_hang` để không dễ dãi hơn thật. (3) `partner-picker.tsx` nhánh nông hộ viết lại dùng **`ChonNongHo`** (chọn hộ có sẵn / **tạo hộ mới thật** có `partyId`, quét CCCD) thay cho "nhập tay tên rời" — hết cảnh phiếu có tên mà không có hồ sơ. (4) Chi tiết phiếu bán **join `getParty`** → hiện tên + SĐT + địa chỉ + nút **"Xem hồ sơ"** sang `/nong-ho/[id]`; phiếu cũ không có `partyId` hiện "Phiếu cũ · chưa gắn hồ sơ". (5) Màn chi tiết nông hộ thêm **"Lịch sử mua vật tư"** (lọc phiếu bán theo `partyId`). (6) Dọn comment **stale** ở `parties.ts`: `POST /parties` **đã có** + `field_staff` **đã có** `party:create` (`core/rbac.js`, đổi chính sách 2026-08) — không còn 404 như comment cũ cảnh báo. `tsc` sạch. |
| _(đã commit)_ | feat | `den-thua`: **Cây xen chọn được NHIỀU cây** — `ThuaDat.cropXen` đổi `string` → `string[]`; component mới `ChonNhieuCayXen.tsx` (tick chip inline + chip "Đã chọn" có nút X + ô "Thêm cây khác" — mượn style [MaChip.tsx](src/features/vat-tu/components/MaChip.tsx)); bỏ Switch xen canh ở [tao-thua.tsx](app/thua/tao-thua.tsx) và [sua/[id].tsx](app/thua/sua/[id].tsx) (list rỗng = không xen). Helper `chuanHoaCay` + `themCayXen` ở [cay-trong.ts](src/features/den-thua/cay-trong.ts) — dedupe theo `trim().toLowerCase()`, chặn trùng cây chính. Hiển thị `· xen ngô, lạc, gừng` ở [id].tsx](app/thua/[id].tsx) + [ThuaDatCard.tsx](src/features/den-thua/components/ThuaDatCard.tsx); search cropXen ở tab Thửa. Seed thửa cà phê `GP-260812-02` có `cropXen: ['Ngô', 'Đậu tương']` để demo. **Cần BE `nag_erp` migration `crop_xen: VARCHAR → TEXT[]`** + sửa `service.createPlot`/`updatePlot` nhận array trước khi tắt mock (hiện real mode gửi array sẽ 400). `tsc` sạch. |
| _(đã commit)_ | feat/fix | `den-thua`: **Gợi ý cây trồng/xen tách riêng + màn "Sửa thông tin thửa"** — (1) Ô **Cây trồng chính** và **Cây xen** dùng **2 danh sách gợi ý KHÁC NHAU** (`cay-trong.ts`: `CAY_TRONG_GOI_Y` cây chính + `CAY_XEN_GOI_Y` cây kèm — ngô/đậu/lạc/ớt/gừng…); `ChonCayTrong` thêm prop `goiY` (ô cây xen KHÔNG gộp danh mục giống). Chặn xen canh trùng cây chính bằng **so tên chính xác** (khác giống = khác cây: chanh leo tím ≠ chanh leo vàng, được xen với nhau). (2) **Màn mới `app/thua/sua/[id].tsx`** — sửa cây chính/cây xen/ngày kích hoạt/ghi chú, lưu qua `updatePlot` (mở rộng nhận `ngayGoc` mock-only; cây xen tắt toggle → clear); nút bút chì "Sửa thông tin" ở thẻ thông tin `[id].tsx` (khác nút "Sửa ranh"). `tsc` sạch. |
| bc5a58d | feat | `den-thua`: **Nhật ký canh tác VietGAP theo giai đoạn cây (demo/mock)** — thay hẳn 4 loại cũ bằng bộ VietGAP `canh_tac`/`bon_phan`/`phun_thuoc`/`thu_hoach`/`tinh_trang_cay` (bỏ `ban_vat_tu`+`tu_van`). Form `nhat-ky.tsx` tự nạp thửa (`getPlot`), tính giai đoạn (`nhanDangCayTrong`+`tinhMocCanhTac`+`chiSoMocHienTai`) → **badge giai đoạn** + chip loại **gợi ý theo giai đoạn** (map `goiYTheoGiaiDoan` ở `lich-canh-tac.ts`, chanh leo điền đủ). Thêm `ngay` (ngày làm việc, `DateField`) + `chiTiet` (túi trường theo loại) ở `types.ts`; sub-form tách riêng `BonPhanForm`/`PhunThuocForm`/`ThuHoachForm`/`CanhTacForm`. Mock `taoNhatKy` tự tính `ngayAnToanThuHoach` = ngày phun + cách ly; thu hoạch soi lần phun chưa hết cách ly → **cảnh báo cách ly mềm** (không chặn). Header hiện Tên hộ · Mã thửa · cây. `tsc` sạch. Backend thật để sau (Stage tiếp). |
| _(đã commit)_ | feat/fix | `den-thua`: **Gọt màn "Tạo thửa" theo phản hồi thực địa** — 5 điểm: (1) **Bỏ chế độ "Nhanh (ước lượng)"** — luôn vẽ ranh trên bản đồ; bản đồ lỗi chỉ còn "Thử lại" (bỏ luôn store flag `yeuCauNhanh` + nút "Khai nhanh thay thế" ở `ve-ranh.tsx`). (2) **Vá "trắng bản đồ"** — ô xem trước ranh (`tao-thua.tsx`) giờ render ảnh vệ tinh thật `BanDoRanh mode="xem"` như màn chi tiết, thay vì SVG nền trắng gây tưởng lỗi; `RanhThuaPreview` chỉ còn là fallback khi mất mạng (`mapPreviewLoi`). (3) **Cây trồng chọn hoặc thêm** — component mới `ChonCayTrong.tsx`: chip từ danh mục giống (`listSeedProducts`) + gõ tự do; giữ `cropName` là TEXT nên khớp lịch canh tác vẫn chạy. (4) **Xen canh** — Switch "Trồng xen canh" + cây phụ, lưu field mock-only `cropXen` (`types.ts`, `growing-areas.ts` — theo tiền lệ `ngayGoc`, không nhét vào `note`), hiện ở màn chi tiết. (5) **Xem trước lịch canh tác** ngay dưới ngày kích hoạt, tái dùng `nhanDangCayTrong`/`tinhMocCanhTac`/`TimelineCanhTac`. Dọn sạch state/import chết của chế độ nhanh; `tsc --noEmit` sạch. |
| 44a29c4 | feat | `den-thua`: **Lịch canh tác theo thửa, ghim góc lấy ranh, geocode thật** — timeline vòng đời cây tính từ ngày gốc (`lich-canh-tac.ts`, mới có lịch chanh leo); vẽ ranh bằng cách ghim từng góc trên ảnh vệ tinh (MapLibre + Esri trong WebView); reverse-geocode ra địa chỉ hành chính thật. |
| 032a2c2 | fix | `den-thua`: **Vá crash expo-audio + thống nhất thuật ngữ "thửa"** — sửa crash khi thu âm nhật ký; chuẩn hoá thuật ngữ "thửa" (thay vì "ruộng") toàn luồng. |
| f30d01b | feat | `den-ruong`: **Dò thửa đất theo GPS, tạo hộ + thửa, nhật ký canh tác** — point-in-polygon dò thửa quanh vị trí đứng, tạo nông hộ + thửa tại chỗ, ghi nhật ký field-visit. |
| 48517f6 | feat | `phase3b`: **Vật tư parity ERP + auth alignment + demo định vị** — đồng bộ nghiệp vụ vật tư với ERP, chỉnh auth cho khớp, thêm demo định vị. |
| a86aecc | fix | `phase3`: **Adversarial review fixes** — vá 22 findings đã verify. |
| 1f5b3eb | feat | `phase3`: **Nhập/Xuất vật tư** — landing, catalog, list, detail, wizard, scan. |
| a050f19 | feat | `phase2`: **Đơn hàng giống** — list + detail + wizard tạo đơn + máy trạng thái (FSM). |
| b9a1a53 | feat | `phase1`: **QR scan + activation flow** + adversarial review fixes. |
| 49b92ce | feat | `phase1`: **Nền tảng auth + login + tab layout** (mock mode). |

## Follow-ups (deferred)

- **Backend `đến-thua`**: đã có ở `nag_erp` — cột `crop_xen` (a5abfc5), `/parties` + PATCH (nông hộ), module **nhật ký `task` field_visit → Mongo** (74dfd75), **và (2026-08-22) trục lịch canh tác**: cột `growing_plot.planted_at` + bảng `crop_catalog`/`plot_milestone` + module `crop-catalog`/`plot-milestone` (migration `2026-08-22_lich_canh_tac.sql`) — **hiện uncommitted ở repo `nag_erp_api`, cần commit riêng**. Perm mới `crop-catalog:edit`/`plot-milestone:confirm` đã cấp cho `field_staff` qua `/rbac` (DB); DB đã seed thì phải cấp qua `/rbac` hoặc reseed, không tự vào từ code DEFAULT.
- **⚠️ Lệch contract nhật ký**: backend `modules/task` (74dfd75) validate `loai` theo bộ CŨ (`ban_vat_tu`…), nhưng mobile đã đổi sang VietGAP (`canh_tac`/`bon_phan`/`phun_thuoc`/…) + thêm `ngay`/`chiTiet`. Cần cập nhật `task/service.js` cho khớp trước khi nhật ký chạy thật (hiện mobile để demo/mock).
- **BE `GET /kho/phieu-*` chưa lọc được**: `svc.listPhieu*()` không nhận tham số, `core/list.js` chỉ cắt offset/limit ⇒ mọi filter (`khoId`/`status`/`partyId`/`q`/khoảng ngày) hiện **không có tác dụng ở real mode**; màn "Lịch sử mua vật tư" của hộ đang lọc client-side theo `partyId`. Cần BE nhận filter để khỏi kéo cả bảng.
- **⚠️⚠️ RBAC: `kho:ban` HIỆN KHÔNG THUỘC VỀ VAI NÀO (trừ admin)** — rà `core/rbac.js`: chỉ `viewer` (`kho:view`) và `seed_producer` (`kho:view/nhap/kiem`) có quyền kho; `seed_producer` bị chú thích *"KHÔNG `kho:ban` (bán là việc ở quầy trạm)"* nhưng **role quầy trạm không tồn tại** trong `DEFAULT_ROLE_PERMISSIONS`. Hệ quả: **toàn bộ luồng bán vật tư chỉ admin dùng được**; `field_staff` (KTV) không vào nổi tab Kho (gọi thật `GET /kho` bằng `dung.pt@nafoods.com` → 403 `cần: kho:view`). **Cần nghiệp vụ chốt ai được bán** rồi BE cấp quyền — đây là việc backend, mobile đã map đúng theo `permissions` nên tự thông khi BE nới.
- **`timThuaTheoToaDo` + `listPlotsKemHo` lọc theo người tạo ở backend thật** (`growing-areas.ts`): NV A đứng trên thửa NV B đã vẽ sẽ không thấy → app kết luận sai "chưa có thửa". Hệ quả tương tự cho **màn Bản đồ thửa** (`den-thua.tsx` chế độ bản đồ) — chỉ vẽ được thửa mình tạo nếu thiếu `growing-area:approve`. Cần endpoint list/tra-theo-toạ-độ bỏ qua ownership (hoặc gate theo quyền xem-vùng).
- **⚠️ Bán lẻ — lớp TIỀN (K4) backend = 0%, đang mock toàn bộ**. Khi nối thật cần:
  - **Thu tiền phiếu bán**: `POST /kho/phieu-ban/:id/thu-tien` `{soTien, phuongThuc, ghiChu?}` → phiếu có `daThu`/`lanThu`; trạng thái TT derive ở client (`features/vat-tu/payment.ts`), không lưu. Xem `api/erp/thanh-toan.ts` (nhánh real đang `throw`).
  - **Khách vãng lai**: BE `kho/service.js` hiện vẫn ném 400 `thieu_khach_hang` khi thiếu `partyId`. Cần chấp nhận `partyKind='khach_le'` + `khachLe{ten,sdt}` (không partyId), và `partyKind='cooperative'` cho HTX. Mock đã nới; client gửi đúng shape.
  - **Đổi trả (`khach_tra`)**: `POST /kho/phieu-tra` + `GET /kho/phieu-tra?phieuGocId=` — sinh moves `huong=in chungTuLoai=khach_tra`, cập nhật `daTra`/hoàn tiền phiếu gốc. Xem `api/erp/phieu-tra.ts`. **Hạn chế đã biết**: giảm giá toàn phiếu KHÔNG phân bổ vào giá trị trả (dùng đơn giá dòng gốc).
  - **Hoá đơn**: hiện chụp ảnh view (`react-native-view-shot`) + `expo-sharing`. BE về sau có thể phát số HĐ / PDF chuẩn.
- **⚠️ Inbox — backend + realtime WebSocket là Phase 3 (README), chưa có**. `api/erp/inbox.ts` **LUÔN dùng mock (KHÔNG gate `MOCK_API`)** — ngoại lệ có chủ đích với Khuôn 4: inbox độc lập, chưa có endpoint nào, cần demo được kể cả khi app trỏ backend thật. Khi nối Phase 3 thêm lại nhánh real gate `MOCK_API`: `GET/POST /inbox/hoi-thoai(/:id/tin|/da-doc)`. Auto-reply hiện là GIẢ (setTimeout 1.5s) chỉ để demo — bỏ khi có BE. Vai thật (quầy vs khách/HTX) do backend quyết; client đang giả lập bằng role `htx`.
- **⚠️ Kho tạm (xe) — BE `POST /kho` chưa có + sync có thứ tự chưa xử lý**. Tạo kho tạm hiện: mock dùng full; real còn 404. Khi nối thật cần: (1) **`POST /kho`** body `{ten,loai:'xe',loaiXe,custodianUserId}` trả Kho trực tiếp (không envelope) + **cột `loaiXe`** (vehicle_kind: xe máy/tải) + **gate quyền BE** (vd `kho:tao`) — client chưa map role (Khuôn 5). `api/erp/kho-sync.ts#flushKhoQueue` đã GIỮ hàng đợi khi 404/50x nên bật BE là tự drain. **Chuyển kho nới cho nguồn=xe (trung chuyển)** → BE `createKeHoach`/`nag_erp` phải chấp nhận nguồn là kho xe + kiểm ràng buộc trung chuyển (hiện mock cho hết). (2) **Sync có THỨ TỰ**: phiếu chuyển/bán tham chiếu kho tạm mang id `LOCAL-KHO-…`; phải chốt kho lên BE **TRƯỚC**, rồi **remap `idThatCuaKhoTam(tempId)`** vào phiếu trước khi gửi. Hiện chặn tạm bằng guardrail `Kho.dongBoTam` (disable submit khi kho chưa đồng bộ) — chỉ an toàn cho mock/online, chưa có offline-write phiếu. (3) Merged list (`useKhoList`) mới áp cho `chuyen-kho`+`ban-hang`; các màn kho khác (`kho.tsx`, `ton-kho`, `kiem-kho`) vẫn dùng `listKho` thuần — kho tạm chưa hiện ở đó (chấp nhận được, chưa cần chọn kho tạm ở đó).
- **Không có `Idempotency-Key`** (WORKFLOW W10): quầy bán lẻ mất mạng bấm lại sẽ đẻ phiếu trùng — chưa bật offline-write cho kho/bán, chờ BE thêm middleware.
- **Lịch canh tác mới có chanh leo**; cà phê / bơ / ổi để trống có chủ đích, chờ nghiệp vụ cấp mốc (`LICH_CANH_TAC`).
- **Offline:** bỏ chế độ "Nhanh" nghĩa là mất mạng/không tải được bản đồ thì không tạo được thửa (quyết định có chủ đích với người dùng).
- **IA — 2 card Vùng trồng cùng đích**: "Thửa quanh bạn" và "Danh sách thửa" (`app/(tabs)/vung-trong.tsx`) đều push `/den-thua` (màn đó chứa cả khối dò GPS lẫn danh sách). Khi cần tách: thêm param `?muc=danh-sach` + scrollTo khối tương ứng ở `den-thua.tsx` (đã ghi TODO trong file).
- **IA — tab mặc định khi mở app**: `app/(tabs)/index.tsx` nay là Trang chủ thật (mở app vào đây). Redirect `'/'` từ login/register/order/vat-tu vẫn về Trang chủ. Nếu muốn mở thẳng vào tab khác, đổi ở tầng route — chưa chốt (user để định nghĩa sau).
- **IA — Trang chủ bỏ "Gần đây" + "Phiếu mới nhất"**: chốt gọn còn 3 rail ngang (cần xử lý · đang làm dở · lối tắt). Store `nag.hoat-dong-gan-day` (con trỏ bản ghi vừa mở, ids-only không PII) đã **revert** cùng đợt này — nếu muốn dựng lại "Gần đây": khôi phục `src/stores/hoat-dong-gan-day.ts` từ git, đăng ký lại ở `wire.ts` + reconcile login/register, gọi `ghiHoatDong` khi mở `thua/[id]`·`nong-ho/[id]`·`ban-hang/[id]`.
